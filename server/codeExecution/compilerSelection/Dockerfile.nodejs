FROM node:latest

# Set the working directory
WORKDIR /compiler/javascript

# Install essential build tools and dependencies for JavaScript compilation
RUN apt-get update && \
    apt-get install -y \
    npm \
    && rm -rf /var/lib/apt/lists/*

# Set entry point to an interactive shell
ENTRYPOINT ["/bin/bash"]
